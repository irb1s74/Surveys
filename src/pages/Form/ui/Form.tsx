import {Container} from "@mui/material";
import {FormReply} from "features/FormReply";

const Form = () => {

    return (
        <Container maxWidth='md'>
            <FormReply/>
        </Container>
    );
};

export default Form;
